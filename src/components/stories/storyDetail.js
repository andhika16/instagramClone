const StoryDetail = ({}) => (
	<div class="absolute w-full h-screen bg-black flex justify-center">
		<img
			class="w-4/5 h-screen"
			src={`https://picsum.photos/200?random=1`}
			alt=""
		/>
		<div class="absolute top-0 bg-black opacity-90 text-white">hello</div>
	</div>
);

export default StoryDetail;
